## Codex-Specific Instructions

When using Codex CLI for this skill:

- Use Codex CLI repo tools to inspect referenced files before writing.
- Prefer direct writes to the expected output path over intermediary scratch files.
