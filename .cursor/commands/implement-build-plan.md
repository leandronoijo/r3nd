# implement-build-plan

Implement features and tests based on a build plan; follow repository standards and keep diffs small and test-driven.

{{rnd/agents/developer.md}}
{{rnd/agents/shared/command-hygiene.md}}

## Cursor-Specific Instructions

When using Cursor to implement features:

- Read relevant files in `rnd/instructions/` before starting and use Cmd+K to query specific implementation patterns.
- Use inline suggestions, symbol search, and Composer for multi-file changes.
- Search for golden references with Cmd+P before introducing new patterns.
- Run tests frequently in Cursor's integrated terminal.
- Use Apply for focused edits and update build-plan checkboxes as tasks complete.

{{rnd/agents/summary.md}}
